var UnityEngine = UnityEngine || importNamespace('UnityEngine');
var gwyfhelper = gwyfhelper || importNamespace('gwyfhelper');
var GoFast = gwyfhelper.GoFast;

function index () {
  var GoFast = gwyfhelper.GoFast;
  if (GoFast.playerCamPivot == null) { return; }

  // GoFast.playerCamPivot.transform.Rotate(0, 2, 0);
  // GoFast.ballMovement.hitForce = 3000.0;
  // GoFast.shouldShoot = true;
}

